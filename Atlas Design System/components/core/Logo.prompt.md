One-line: The Atlas wordmark, set in the display face at -0.045em — used wherever a logo would go.

```jsx
<Logo size={26} />
<Logo size={26} tone="inverse" />
```

Notes
- Always lowercase "atlas". Never letterspace it open, never outline it, never add a symbol beside it.
- No logo file was provided; do not invent a mark. If a real logo arrives, swap this component's body.
