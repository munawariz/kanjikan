One-line: The four-point lime star that separates marquee items and punctuates Atlas headlines.

```jsx
<Sparkle size={18} />
<Sparkle size={16} color="var(--forest-800)" />
```

Notes
- Never more than one per headline, and only as a separator inside a Marquee.
- Lime on forest grounds; forest on white/cream.
