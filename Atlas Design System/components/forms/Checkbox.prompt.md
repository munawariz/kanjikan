One-line: Checkbox with optional label and helper line; forest box, lime tick.

```jsx
<Checkbox checked={on} onChange={() => setOn(!on)} label="Save this recipient" description="Appears in your quick-send list." />
```

Notes
- 22px box, 6px radius, 12px gap to the label. The tick is lime, never white.
