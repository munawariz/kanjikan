One-line: The Atlas text input — 52px tall, hairline border, forest border and lime focus ring on focus.

```jsx
<Input placeholder="Search transactions" icon="search" />
<Input defaultValue="120.00" suffix="USD" />
```

Notes
- Pair with `Field` for labels and errors. `invalid` turns the border red.
- Disabled fills with `--ink-100`; do not grey the text further.
