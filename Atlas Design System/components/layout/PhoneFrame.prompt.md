One-line: The device shell Atlas uses to show app screens inside marketing sections.

```jsx
<PhoneFrame width={320}><TransactionsScreen /></PhoneFrame>
```

Notes
- Forest bezel by default (10px at width 300), sage on lighter cards.
- Screens are usually cropped by the card that contains them — let the frame bleed off the bottom edge rather than shrinking it.
