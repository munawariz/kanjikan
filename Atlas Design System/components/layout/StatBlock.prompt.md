One-line: A proof number — display-face figure with a small semibold caption beneath.

```jsx
<StatBlock value="7.5M" label="Total daily transactions" />
<StatBlock value="+2%" label="Unlimited daily cashback" size="md" />
```

Notes
- 4px gap between figure and caption. Figures never get colour; the caption never gets uppercase.
- Group two or three per cluster, stacked vertically with 32-40px between.
