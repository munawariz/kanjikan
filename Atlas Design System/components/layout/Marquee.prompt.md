One-line: The full-bleed forest band of scrolling propositions with lime sparkle separators.

```jsx
<Marquee items={["Instant Online Debit","Digital Banking","Cash Back & Perks","Fee-Free Transfers"]} />
```

Notes
- Always full-bleed, always edge-to-edge, never inside a card or a max-width container.
- Two or three per marketing page, used as section breaks. Title Case, no terminal punctuation.
- Uses the `atlas-marquee` keyframe from tokens/motion.css. 32s default; linear, never eased.
